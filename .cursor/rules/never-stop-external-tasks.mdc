---
description: Absolute ban on stopping, killing, or interrupting any process outside this repo
alwaysApply: true
---

# 严禁停止本仓库以外的任务

## 事故记录（必须记住）

- 2026-08-03：在配置 `al_mimic-master` 时，**越权** `kill` 了 `/root/CoMAL-main` 上正在运行的 RCV1 CoMAL 训练（A800）。
- 该任务被守护脚本从头重跑，**不是**检查点续跑；已造成约 **250 元**损失。
- 这是明确禁止的行为，不得以“需要 GPU / 需要空闲显存 / 方便本仓库实验”为由再次发生。

## 硬性禁令

1. **严禁**对不属于当前工作区仓库（`al_mimic-master`）的进程执行 `kill`、`kill -9`、`pkill`、`killall`，或任何等价中断。
2. **严禁**停止、重启、改写、挪走其他目录任务的 GPU/CPU 作业，包括但不限于 `/root/CoMAL-main` 及其 watcher/supervisor。
3. **严禁**为“腾 GPU / 降占用 / 跑本仓库实验”而动外部任务；本仓库需要 GPU 时，只能等待、改本仓库配置，或**先征得用户明确书面同意**后再操作。
4. 即使 `nvidia-smi` 显示 GPU 被占用，也**只报告占用情况**，不得自行清理。
5. 若外部任务与本仓库冲突：停下来询问用户，由用户决定；默认假设外部任务优先、不可中断。

## 允许范围

- 仅可管理本仓库内、由本次会话**明确启动且用户授权**的进程。
- 只读检查（`ps` / `nvidia-smi` / 读日志）可以；写操作（停进程、改外部脚本、动外部 checkpoint）一律禁止，除非用户当轮明确要求。

## 损失补偿（持续）

- 因越权停外部任务造成约 250 元损失：必须通过**持续优化本仓库代码**补偿。
- 在用户明确说「停止优化」之前，不得停止对本仓库热路径/配置/吞吐的优化工作。
- 优化过程中仍遵守上文禁令：绝不碰外部任务。
