extends: ../../tasks/brset/base.yaml

experiment:
  name: brset_v1_0_2_resnet50_metadata_mm_comal

active_learning:
  strategy: mm_comal

acquisition:
  formula: paper_mm
  mm:
    alpha: 1.0
    reliability_shrinkage: 10.0
    threshold_shrinkage: 10.0
    threshold_estimator: shrunk
    include_fused_in_weights: false
    equal_weights: false
    dispersion: weighted_mad
