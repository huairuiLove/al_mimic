extends: ../../tasks/brset/base.yaml

experiment:
  name: brset_v1_0_2_resnet50_metadata_modis

active_learning:
  strategy: modis

modis:
  beta: [1.0, 1.0, 1.0]
  oof_folds: 5
  probe_epochs: 20
  probe_batch_size: 64
  probe_eval_batch_size: 512
  probe_learning_rate: 0.0001
  probe_weight_decay: 0.0
  prototype: mean
  grid_k: 8
  bisect_steps: 0
  workset_size: 5000
  fusion_batch_size: 512
